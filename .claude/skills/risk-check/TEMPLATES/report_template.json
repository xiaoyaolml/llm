{
  "report_id": "{{skill}}-{{timestamp}}",
  "skill": "risk-check",
  "generated_at": "{{timestamp_iso}}",
  "target": {
    "repository": "{{repository}}",
    "branch": "{{branch}}",
    "commit": "{{commit_hash}}",
    "scope": "{{scope}}",
    "path": "{{target_path}}"
  },
  "summary": {
    "status": "{{status}}",
    "decision": "{{decision}}",
      "files_changed": "{{files_changed}}",
      "blocking_count": "{{blocking_count}}",
      "non_blocking_count": "{{non_blocking_count}}"
  },
  "domain_context": {
    "risk_type": "{{risk_type}}",
    "method": "{{calculation_method}}",
    "data_window": "{{data_window}}",
    "business_constraints": "{{business_constraints}}"
  },
  "blocking_issues": [
    {
      "id": "{{issue_id}}",
      "category": "risk-correctness",
      "severity": "critical",
      "file": "{{file_path}}",
      "line": "{{line_number}}",
      "title": "{{title}}",
      "risk": "{{risk}}",
      "evidence": "{{evidence}}",
      "recommendation": "{{recommendation}}",
      "business_impact": "{{business_impact}}"
    }
  ],
  "non_blocking_issues": [
    {
      "id": "{{issue_id}}",
      "category": "risk-correctness",
      "severity": "medium",
      "file": "{{file_path}}",
      "line": "{{line_number}}",
      "title": "{{title}}",
      "risk": "{{risk}}",
      "evidence": "{{evidence}}",
      "recommendation": "{{recommendation}}",
      "business_impact": "{{business_impact}}"
    }
  ],
  "evidence_sources": [
    {
      "type": "baseline|sample|backtest|report|test",
      "reference": "{{reference}}",
      "note": "{{note}}"
    }
  ],
  "validation_plan": [
    {
      "name": "{{validation_name}}",
      "goal": "{{validation_goal}}",
      "method": "{{validation_method}}",
      "success_criteria": "{{success_criteria}}"
    }
  ],
  "follow_up": [
    {
      "priority": "{{priority}}",
      "action": "{{action}}",
      "owner": "{{owner}}"
    }
  ]
}
